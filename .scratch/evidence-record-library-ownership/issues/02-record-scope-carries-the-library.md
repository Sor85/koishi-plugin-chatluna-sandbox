# 02 — 记录域带上记录库，跨记录域读取与轨迹不再穿控制服务

**What to build:** 全部空间视图下读一条记录详情、以及在主模拟 QQ 环境、AI 测试空间、未归属三种来路下读模型请求轨迹，行为一字不变，但路径少一层。

今天这两条路都写成「取记录域 → 取控制服务 → 取记录库 → 读记录」，穿两层去拿；轨迹那条还要按记录来路在三者之间三层嵌套地挑记录库。本票让记录域自己带上记录库——未归属那一支今天已经带了，场景那一支补上——并由记录域目录提供「按记录域标识取记录库」这一件事，那段三元表达式塌成一次查询。

控制服务上取得记录库的入口保留，但从此只服务于插件入口的装配，不再有别的消费方。

那个可辨识联合不动：未归属仍然只有记录库、没有沙盒场景，跨记录域查找仍然只吃场景那一支，「未归属永不参与联邦读取、必须显式指定范围才能读到」一行不改。记录域目录那条 ADR 只补一句形状说明，既有决定一条不推翻。也不要顺手把可选字段换成判空——上一轮明确判过，可选字段只是把判空搬进类型里。

**Blocked by:** None — can start immediately.（与 01 无依赖，但两票改的是同一个详情读取函数的不同分支，推荐在 01 之后做以省一次重复编辑。）

**Status:** resolved

- [x] 场景那一支的记录域直接给出记录库，从记录域读一条记录不经控制服务，有断言
- [x] 「按记录域标识取记录库」有断言：主模拟 QQ 环境、AI 测试空间、未归属三种来路各一次
- [x] 三种来路下的轨迹读取结果与今天一字不变，有断言
- [x] 全部空间视图下的详情读取与未命中提示一字不变，有断言
- [x] 未归属仍然不参与联邦读取、仍然必须显式指定范围，既有断言一字不改地通过
- [x] 可辨识联合未被改成可选字段
- [ ] 控制服务上取得记录库的入口只剩装配用的那一个消费方 —— **按判定不采纳**：这条验收项本身推错了，理由见 Comments「这条要求为什么不采纳」
- [x] 记录域枚举那条既有守卫规则未被本票的改动命中
- [x] 记录域目录那条 ADR 补上形状说明，既有决定一条未改
- [x] Console RPC 契约逐字不变
- [x] MCP 工具清单与 schema 逐字不变
- [x] stash 基线比对记录详情、轨迹与跨记录域查找的完整输出，逐字节相同
- [x] `git diff --stat` 里没有客户端路径
- [x] 别处变红的断言逐条区分真红与假红，处置记入 Comments，不重判前几轮判定保留的断言
- [x] 完整测试、类型检查与构建通过

## Comments

### 落地形状

- `SceneScope` 多一个 `records: SandboxModelRequestStore`，与 `UnattributedScope` 同名同义，
  `RecordScope` 两支因此都能直接给出记录库。`control` 原样保留：等就绪、清场景仍归它。
- 记录域目录新增 `getModelRequests(scopeId)`，实现是 `listScopes().find(...)?.records` 一句。
- 跨记录域详情读取（Console 与 MCP 各一处）从 `scope.control.getModelRequestStore().getRecord(...)`
  变成 `scope.records.getRecord(...)`。
- 轨迹那段按来路的三层嵌套三元塌成一次查询：来源标注反过来给出记录域标识
  （`modelRequestScopeId`），记录库按这个标识取一次。三种来路只影响标识的值，不再影响分支数。
- 装配侧同样收敛：`getCandidates` 与用量关联的记录库清单都直接读 `records`；预设服务的
  `getTestSpaceModelRequests` 改成 `scopes.getModelRequests(spaceId)`，原先那段
  `spaceId === 'main' ? … : try { getControl(spaceId).getModelRequestStore() } catch {}` 整段消失。

### 这条要求为什么不采纳

事实先摆清楚。`control.getModelRequestStore()` 现在有五处调用：

- 记录域目录构造两处（主环境、AI 测试空间各一处）—— 装配
- 预设服务装配一处（`mainModelRequests`）—— 装配
- **单记录域详情读取两处**：Console 的 `resolveModelRequestControl(input)!.getModelRequestStore()`、
  测试控制端点的 `resolveControl(args, false).getModelRequestStore()`

前三处符合本票的判定。后两处保留，理由是那条路上解析出控制服务**不是为了取记录库**：它同时判定
「这个空间存在吗」与「你有权动它吗」，并按各自的错误契约拒绝。改成按标识取记录库会跳过这段判定，
未知空间会从结构化错误退化成「记录不存在」，与「对外可观察行为逐字不变」冲突。保留它等于承认
这条路上先解析出控制服务是权限需要，取记录库只是权限确认之后的最后一步，而不是穿两层门去读。

原先这里写的「正确做法是把『按范围解析出权限已校验的记录库』变成一个独立成员」**站不住，作废**。
两处的权限判定不同源，拒绝口径也不同：Console 那条路的范围解析写在注册函数的闭包里，拒绝时抛
普通错误或领域错误；测试控制端点那条路写在测试控制服务自己身上，拒绝时按错误码契约抛结构化错误
（`space_id_required`、空间不存在）。没有一个成员能同时服务两侧——它们要的不是同一种「拒绝」。
真要收只能两侧各自把「解析出范围 → 拿到记录库」并成自己的一步，而两侧各自只有一两个调用点，
省掉两次 `.getModelRequestStore()` 却各添一个名字，这笔账是负的。

因此这条不是一笔待办债务，而是一条推错了的验收项：它从「查记录不该绕前台」这个真实观察
（本票已经做到——跨记录域读取与轨迹都不再穿控制服务）顺手推出「那个入口应该只剩装配一个消费方」，
推的时候没看到那两处不是绕路。留成待办的代价是下一轮有人照着做一遍，发现两侧口径不一致，白花一轮。

### 顺带记下一个真实的隐患

「取记录库」与「验权限」现在是分开的两步，新增调用点可以只做后半步而漏掉前半步，票 03 的转售
守卫拦不住这个——它只看公开成员体的形状，不看调用点有没有先验权限。

这个隐患的归宿是后续候选「控制服务按职责划内部 module」：那条候选要先决定权限判定归谁，决定之后
「按范围拿到已验权限的记录库」才有一个站得住的位置。它不是本票这两处调用点要解决的问题。

### 既有决定一条未推翻

- 可辨识联合未改成可选字段：未归属仍然没有 `control`，场景那一支仍然没有 `records?`。
- 未归属仍然不参与联邦读取（`listScenes` 不含它），既有断言「联邦只遍历沙盒场景」一字未改地通过。
- 「未命中返回空值、持久化故障照原样抛出」一字未改，既有断言继续通过。
- ADR-0083 只补一段形状说明，既有段落一字未改。

### stash 基线

`evidence/baseline.ts` 的输出与票 01 完成后逐字节相同（`diff` 无输出），因此本票是纯结构性的。

### 别处变红的处置

零处。本票没有改动任何既有断言，1632 个用例（含票 02 新增的 4 个）全绿。

### 验证

`yarn typecheck`、`npx vitest run`（179 文件 / 1632 用例）、`yarn build` 全部通过。
`git diff --stat` 只含 `src/`、`tests/` 与 `docs/adr/`，无 `client/` 路径。
