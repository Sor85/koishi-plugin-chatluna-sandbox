# 02 — 把工作台内视图返回栈并入证据导航 module

**What to build:** 从轨迹账本进入检查器再返回时,选中行、账本滚动位置、详情面板滚动位置、页签与轨迹模式仍然按离开时的样子恢复;跨请求返回时仍然等目标详情按记录身份真正到达后才恢复,中途的详情变更不会把恢复消费在错误的记录上。用户观察到的行为完全不变。

变化在归属:工作台内视图返回栈的状态与它的一次性触发编号并入证据导航 module,成为第四个编号。返回栈 module 删除。合并后客户端不再有任何位于 module 之外的往返触发编号——四个编号全部由证据导航 module 独家发出,视图与工作台 shell 一个都不持有。

四个编号不合并成一个:工作台内恢复必须等目标详情按记录身份到达,预设工作台原点恢复在视图切换后立即生效,两者的等待条件不同;合并会迫使两个组件观察同一个编号并各自判断这一次轮不到自己。

**Blocked by:** 01 — 建立证据导航 module 并统一进入模型请求视图的路径

**Status:** resolved

- [x] 从轨迹账本进入检查器后可以返回轨迹,原选中行被恢复
- [x] 返回轨迹时账本滚动位置被恢复
- [x] 返回轨迹时详情面板滚动位置被恢复
- [x] 返回轨迹时页签与轨迹模式被恢复
- [x] 跨请求返回时等目标详情按记录身份到达后才恢复视图快照
- [x] 同时可从轨迹和预设返回时,返回入口指向最近一次离开的位置
- [x] 自动刷新期间返回状态不被打断,仍然存在的选中行被保留
- [x] 工作台内视图快照的恢复编号由证据导航 module 发出,成为其第四个编号
- [x] 四个编号语义互不重叠,各自单调,消费者不需要判断这一次轮不轮到自己
- [x] 客户端不再有位于证据导航 module 之外的往返触发编号
- [x] 返回栈 module 删除,无残留引用
- [x] 返回栈既有断言并入统一测试文件,包含「按记录身份匹配才消费待恢复快照」这条规则
- [x] 不引入 jsdom、happy-dom 或组件挂载测试
- [x] 单元测试、类型检查与构建全绿
- [x] 浏览器验证:轨迹账本到检查器再返回,含选中行、账本滚动、详情滚动与页签恢复

## 实现记录

- 工作台内视图返回栈并入 `createEvidenceNavigation()`：`pushViewSnapshot` / `beginViewReturn` / `takeViewRestore`，恢复编号为第四个（`viewRestoreSeq`）。
- `returnTarget` 计算属性统一了「返回按钮指向哪」：工作台内快照存在时为 `'view'`，否则预设原点存在时为 `'presets'`。工作台的 `canReturnToTrajectory` / `canReturnToPreset` 由它派生，不再是手写 ref。
- 轨迹视图 `restoreState` 改为直接接收 `EvidenceViewRestore`，读取 `state.trajectory.rowId` 与 `state.trajectory.scrollTop`。
- 浏览器验证（Chrome）：轨迹账本选中 `TOOL DEFS 工具定义 · set_self_profile`，账本滚动 400、详情滚动 200，点「打开原始请求」后返回，四项（选中行、账本滚动、详情滚动、页签）全部按离开时恢复。
- 组成分段点击连续两次（System 2.99% → Tool Defs 2.64%）都真的重新定位，详情滚动到 3756。控制台 0 错误 0 警告。
