# 01 — 建立证据导航 module 并统一进入模型请求视图的路径

**What to build:** 从预设工作台跳到模型请求变量后,左侧模型请求列表正确高亮目标记录;目标记录超出列表首屏分页时,被权威详情补入列表。这条已核实的缺陷因此修好,而且是结构性修好——从聊天消息进入与从预设工作台进入共用同一份进入状态,不可能再出现一条路径有列表选择保护、另一条没有。

新建证据导航 module,取代预设证据导航、预设导航协调器与模型请求导航三个 module。它拥有跨视图往返的意图发布与消费、到达闸门、预设工作台原点快照与恢复,以及「本次筛选变化不清空导航选中」这个一次性令牌。进入模型请求视图的意图统一为一个类型两种形态:必含空间归属与记录标识,可选携带证据定位段;携带时走到达闸门并显示正在定位的进行态,不携带时打开记录后立即消费。

module 以响应式引用暴露状态,工作台 shell 不再手工把返回是否可用镜像进多处引用。筛选、页签、选中记录与状态文案继续由视图持有,module 只交出「该进入什么状态」的数据,由视图写入并发起读取。

三个一次性触发编号归入 module:打开某条记录、在已打开详情内定位到某条证据、恢复预设工作台原点。轨迹视图原本持有的本地定位编号消失——详情内定位的三个来源（预设跳转、轨迹账本选中行、请求组成分段点击）从同一个发号源取号,两个互不知情的计数器喂同一个被观察字段的撞号风险因此在结构上消除。预设原点恢复的编号由 module 的返回动作自己发出,不再由工作台 shell 用第二个计数器补上。

工作台内视图返回栈本票不动,它的编号是唯一残留的外部计数器,由下一票收走。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 从预设工作台跳到模型请求变量后,左侧列表高亮目标记录
- [ ] 目标记录超出列表首屏分页时被补入列表,左侧仍可选中并切换到相邻请求
- [ ] 从聊天消息进入与从预设工作台进入的列表行为完全一致
- [ ] 两条入口路径共用同一份进入状态,不再各自决定筛选重置、页签、请求范围构造与三次读取
- [ ] 进入状态本身声明「这是一次导航选中」,列表选择保护随之生效,无法被某条路径遗漏
- [ ] 意图为一个类型两种形态:必含空间归属与记录标识,可选携带证据定位段
- [ ] 携带证据定位段时显示正在定位的进行态文案,定位成功后变为已定位,失败时给出明确说明而不是停在进行态
- [ ] 不携带证据定位段时不显示定位文案,意图在打开记录后立即消费
- [ ] 到达闸门三个条件全部满足才交出定位请求:详情标识匹配、轨迹处于单请求模式、该记录在轨迹记录集合内;已交出后不重复交出
- [ ] 轨迹处于会话模式时不触发单请求定位
- [ ] 连续多次跳转每次都真的重新定位
- [ ] 用户主动点选其他请求时取消待处理的跳转
- [ ] 切换到其他视图后再回来不残留上次的跳转意图
- [ ] 返回预设工作台时列表滚动位置、搜索词与源码编辑器滚动位置被恢复
- [ ] 预设原点恢复的编号由 module 的返回动作自己发出
- [ ] 详情内定位的三个来源共用同一个发号源;轨迹视图不再持有本地定位编号
- [ ] 返回是否可用作为响应式事实暴露,工作台 shell 不再手工镜像
- [ ] 一次性令牌移交 module 并与进入状态绑定,消费即失效;原先用于对齐两个 module 的帧后回调、列表选择状态上的令牌字段与释放函数一并删除
- [ ] 模型请求列表选择 module 收窄为普通列表选择与「用权威详情补入超出首屏分页的目标记录」两件事
- [ ] 预设证据导航、预设导航协调器、模型请求导航三个 module 删除,无残留引用
- [ ] 三个旧 module 的既有断言迁入统一测试文件;列表选择既有令牌用例中的规则部分随令牌迁移
- [ ] 新增回归:断言两条入口路径交出的进入状态在保护标记上完全一致（不是断言令牌规则本身——那已有既有用例覆盖）
- [ ] 不引入 jsdom、happy-dom 或组件挂载测试
- [ ] 单元测试、类型检查与构建全绿
- [ ] 浏览器验证:预设工作台到模型请求变量再返回,含列表高亮、跨页补入、返回后列表滚动与搜索词与编辑器滚动恢复
- [ ] 浏览器验证:聊天消息到模型请求,含列表高亮与跨页补入
