# 05 — 加入原始模型证据切换并完成真实环境验收

**What to build:** 让用户能够在分析页的格式化卡片与对应原始模型证据之间核对，并能切换查看完整请求 JSON；同时确保自动刷新、主题、窄屏和独立轨迹页在真实 Koishi 环境中保持稳定。

**Blocked by:** 04 — 将分析页轨迹导航接入模型请求对话视图

**Status:** ready-for-agent

- [ ] 请求消息卡片右上角显示语义正文字符数和 SVG 原始内容切换按钮
- [ ] 请求消息卡片在当前卡片内切换格式化内容与对应原始 JSON 节点
- [ ] 原始消息模式显示低强调度源路径，并复用现有可折叠 JSON 树
- [ ] 原始消息 JSON 中的图片节点继续支持图片预览和原始值切换
- [ ] 响应卡片显示字符数、JSON/SSE/TEXT 来源标签和 SVG 原始内容切换按钮
- [ ] JSON 响应原始模式显示原始 JSON 对象，TEXT 响应显示原始文本，SSE 响应显示保留 event/data 边界的完整事件数组
- [ ] 卡片按钮 Tooltip 在“查看原始 JSON”和“查看格式化内容”之间切换
- [ ] 每张卡片的格式化模式和原始模式分别保存展开状态，切换不影响其他卡片
- [ ] Conversation 工作区标题提供 SVG 完整请求 JSON切换按钮
- [ ] 完整请求模式在右侧内容区显示现有可折叠 JSON 树，根节点展开、子节点折叠
- [ ] 完整请求模式保留左侧导航，返回 Conversation 后恢复原滚动位置和卡片状态
- [ ] 请求体无法解析时显示明确失败状态和完整请求 JSON入口，不生成猜测消息
- [ ] pending 自动刷新以模型请求记录 ID 保持滚动位置、卡片模式、展开状态和工具状态，响应完成后原地更新
- [ ] 删除上一轮错误新增的独立模型请求概览组件及其错误布局样式，不保留后备视图或兼容判断
- [ ] 完整项目测试、类型检查和构建均通过
- [ ] 修改生效前完整重启 Koishi 开发环境，确认旧主进程、worker 和相关端口均已清理且只运行一个新实例
- [ ] 在 Chrome 浅色、Chrome 深色、Firefox 浅色、Firefox 深色及窄屏环境验证 Messages、Response、Tools、搜索、定位、折叠、原始模式和图片预览
- [ ] 验证独立轨迹页面的检查器、原始请求跳转和状态恢复未发生回归
- [ ] 浏览器验收结束后关闭自动化页面、上下文和进程，并清理浏览器工具生成的临时文件
