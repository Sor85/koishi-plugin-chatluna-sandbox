# 02 — 提及菜单与按键路由下沉

**What to build:** 「输入 @ 之后会发生什么」以及「这一次按键是什么意思」变成可以单独驱动的判定。

菜单部分：什么时候开、什么时候关、候选怎么按已输入的片段过滤、选中一个候选后提及插到哪里、以及插完光标落在提及之后而不是跳走。

按键部分三条分流：菜单开着时方向键上下移动候选、Enter 选中候选、Esc 只关菜单；菜单关着时 Enter 才是发送。加上退格——删到提及边界时整块删掉而不是留下半截。

这一轮开始，草稿宿主回答「这次按键有没有被菜单消费」，组件只在未被消费时才走发送。这条边把「Enter 到底发送还是选中」从组件里的一个内联条件变成一个可断言的答案。

「组字期间不开候选菜单」有断言——输入法未上屏时弹出候选会把拼音当成提及片段去过滤。

界面一字不变。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 菜单开合的判定有断言，含输入 @ 后开、片段中出现空白后关、候选为空时不开
- [ ] 候选过滤有断言，含按名称与按关键字两种命中
- [ ] 选中候选后提及插入位置与光标落点各有断言
- [ ] 从别处传入的提及被插到当前光标处，有断言
- [ ] 方向键在菜单内移动、Enter 选中、Esc 只关菜单，三条各有断言
- [ ] 菜单关着时 Enter 走发送，有断言
- [ ] 退格跨提及整块删除有断言，含提及位于开头与位于两段文本之间
- [ ] 「这次按键有没有被菜单消费」由草稿宿主回答，组件不自己判
- [ ] 「组字期间不开菜单」有断言
- [ ] 逐条分类表落盘并核对断言总数；先删除再拆分用例
- [ ] 那个混了三个主题的用例拆成回复上下文、附件浮层、内联提及三个
- [ ] 组件的 props、emits、事件名与载荷逐字不变
- [ ] DOM 快照与基线逐像素一致，光标快照与基线一致
- [ ] 别处变红的断言逐条区分真红与假红，处置记入 Comments
- [ ] 完整测试、类型检查与构建通过
