# 02 — 删除源码断言并按主题拆分测试用例

**What to build:** 对模型请求工作台做等价重构时,测试不再假红。把三处相同的恢复赋值合并成一个函数、改变状态默认值的表达方式、调整组件目录结构、更换图标——这些都不会再让测试变红,而真实行为回归仍然被守住。测试失败时能定位到具体主题,不再是三百多条断言共用同一个用例名。

该测试的第一个用例目前有三百四十八条断言,全部打在读进来的源码文本与样式文本上,横跨导航、筛选、列表、详情、错误诊断与样式六个主题。其中一百零二条把实现写法写进契约:断言某条赋值语句恰好出现两次、断言响应式引用的字面初始化写法、断言从哪个路径导入组件、断言用了哪个图标组件名、断言内联的手势阈值与缩放常量。这类断言在任何等价重构时变红,却对真实行为回归保持沉默。

删除一百二十七条:十六条已有真正执行代码的行为测试覆盖同一事实、一百零二条肯定式实现细节断言、九条与别处重复或只断言图标组件名。剩余二百二十一条保留并按主题拆成多个测试用例。

保留与删除的判据是成本结构而不是断言形式。样式文本断言与用户文案断言不锁死实现写法,改一个变量名不会让它们变红,它们的问题是覆盖弱而不是有害;在不引入组件挂载测试的前提下,用户文案断言是唯一能守住界面文案的手段。否定式的已删除实现守卫只在有人把已删实现加回来时才变红,那正是需要的行为——其中一部分是模型证据投影唯一化与证据导航统一两条工作刚刚加入的。描述可见 DOM 结构与元素顺序的断言约束的是用户能看到的排列,保留。

删除依据是一份已完成的逐条分类,落盘于本目录的 `assertion-classification.md`:十六条可删断言各自附有覆盖它的行为测试文件与行号,一百零二条实现细节断言各自附有它本该由什么 interface 承担。实施时按该分类逐条处理,不重新判断。

该分类表的行号是分类当时的行号。实施前先核对首个测试用例的断言总数是否仍为三百四十八——若已变动,表中行号全部失效,需要重新分类。

本票不改动任何生产代码,界面行为与文案完全不变。

**Blocked by:** None — can start immediately

**Status:** resolved

- [x] 必须先删除再拆分:删除依据是按原始行号编制的分类表,先拆分会让行号整体位移、分类表失效
- [x] 删除十六条已有行为测试覆盖同一事实的断言
- [x] 删除一百零二条肯定式实现细节断言,包括断言赋值语句出现次数、响应式引用字面初始化写法、组件导入路径、图标组件名、内联手势阈值与缩放常量、内联 SVG 路径字面量
- [x] 删除九条与别处重复或只按图标组件名断言的 DOM 类断言
- [x] 保留全部样式文本断言
- [x] 保留全部用户文案与展示语义断言
- [x] 保留全部否定式已删除实现守卫断言
- [x] 保留描述可见 DOM 结构与元素顺序的断言
- [x] 断言总数从三百四十八降到二百二十一
- [x] 剩余断言按主题拆成多个测试用例:页面入口与装配、列表筛选与排序、详情概览与元信息、错误诊断、请求体与响应视图、轨迹与组成图、JSON 树交互、样式
- [x] 每个测试用例的名字说清它断言的是什么
- [x] 该区域仍然只有一个测试文件负责,不拆成多个文件
- [x] 删除断言后若某个源码读取变量不再被引用,一并清理
- [x] 不改动任何生产代码;界面行为与文案不变
- [x] 不引入组件挂载测试、jsdom 或 happy-dom
- [x] 单元测试、类型检查与构建全绿
- [x] 不做浏览器验证:生产代码零改动,行为不可能变化;被删除的断言本来也不验证行为

## Comments

### 分类表行号已漂移，按提交差异重建映射而不是重新分类

实施前核对断言总数：首个用例已是 **370 条**，不是分类表记录的 348。分类表落盘于 `96157c3`，此后 `de6894d`（证据词汇收敛）改动了同一区域，净增 22 条。

没有整表重新分类。做法是把分类表的行号按提交差异整体重映射：以 `96157c3` 版本为旧序列、当前版本为新序列做行级对齐，旧行号逐条映射到新行号，再按分类表原有的处置口径删除。127 条待删旧行里 126 条有对应新行，唯一失配的旧行是被 `de6884d` 就地替换的缩放控件断言。

只有 `de6894d` 新增的 27 行需要新判断，判据沿用分类表的成本结构口径，且尽量对齐它们各自前身的归类：

- **保留 25 条**：粘性轨迹头的 12 条（2 条 class 绑定 + 10 条样式文本，与既有 `'is-selected'` class 绑定同形态，分类表把后者归入保留）、工具栏顺序 1 条、`'is-expanded'` 否定式守卫 1 条、缩放控件样式 8 条（前身旧行属样式文本类）、`evidenceTitleLabel('variable')` 与 `sandboxEvidenceLabels(kind).badge` 各 1 条（前身旧行属用户文案与展示语义类，且是证据词汇收敛那条工作唯一的单一所有权记录）、`kindLabel` 否定式守卫 1 条。
- **删除 2 条**：`<template v-if=...>` 那条按图标组件名与源码先后顺序断言缩放控件——它是分类表明确要删的旧行的原地替换，缺陷同构；以及 `COMPOSITION_ZOOM_MIN = 1`，内联缩放常量写法，且在文件里与另一处已列入删除的同款断言重复。

因此实际删除 **128 条**（126 条映射行 + 上述 2 条），断言总数 **370 → 242**。分类表的「348 → 221」在漂移后对应的就是 242：221 条旧保留里有 4 条被 `de6894d` 就地替换，加上 25 条新保留即 217 + 25 = 242。

### 顺带清理

- 删掉多行模板字面量断言 `emit('open-request', {...})` 时，其续行（原文件 242–247 行）不含 `expect(`、不在分类表计数内，机械删首行会留下语法残骸，一并删除。
- `shellSource` 在唯一引用它的断言被删后不再被使用，移除该读取。
- 一条注释描述的是被删的 `toHaveLength(3)` 断言，删除；另一条描述概览格三态占位符的注释在三条占位符断言删除后改写为只描述剩下的否定式守卫。

### 用例拆分

拆成 10 个用例，覆盖票面要求的八个主题；「轨迹与组成图」按定位价值再分为请求组成图、轨迹账本与检查器、跨视图往返三个，避免单个用例又回到近百条断言共用一个名字。仍然只有这一个测试文件负责该区域。

页面入口与装配 7 / 列表筛选与排序 28 / 详情概览与元信息 35 / 错误诊断 14 / 请求体与响应视图 27 / 请求组成图 23 / 轨迹账本与检查器 49 / 跨视图往返 12 / JSON 树交互 23 / 样式 24 = 242。

验证：`yarn vitest run tests/webqq-model-request-workspace.test.ts` 15 passed（10 个新用例 + 5 个既有行为用例）；`yarn test` 128 files / 843 tests 全绿；`yarn typecheck` 无输出；`yarn build` 成功。生产代码零改动，未做浏览器验证。
