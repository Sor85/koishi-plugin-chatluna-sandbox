# 08 — 切换默认场景并完成全量回归

**What to build:** 将默认场景、测试 fixture 和所有剩余调用方完全切换到统一参与者与逻辑会话结构，删除重构期间残留的旧字段、旧会话 ID 和 fallback，并通过完整自动化与浏览器验证证明新模型可以作为后续 MCP 和 AI 测试空间的稳定基础。

**Blocked by:** 05 — 迁移 OneBot 与 ChatLuna 到逻辑会话；06 — 收紧 Console RPC 与未来 MCP 控制合同；07 — 统一 WebQQ 当前操作者视角

**Status:** ready-for-agent

- [ ] 默认场景包含可直接测试消息、关系申请和群权限的普通用户、机器人与群组，并只使用新参与者和逻辑会话结构
- [ ] 测试 fixture、场景状态和浏览器本地状态不再包含旧双目录、旧身份字段、镜像私聊或成员机器人组合群聊
- [ ] 项目中不存在为旧场景、旧 RPC、旧 localStorage 或旧会话 ID 提供的兼容解析、可选字段或 fallback
- [ ] 回归测试覆盖用户与机器人操作者、三类私聊组合、多机器人群聊、关系删除恢复、退群重新入群和群主权限
- [ ] 完整测试、类型检查、生产构建和差异检查全部通过
- [ ] 使用真实浏览器验证操作者切换、会话列表、消息方向和主要交互，完成后关闭浏览器并清理临时文件
