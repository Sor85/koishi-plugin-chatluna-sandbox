# 08 — 切换默认场景并完成全量回归

**What to build:** 将默认场景、测试 fixture 和所有剩余调用方完全切换到统一参与者与逻辑会话结构，删除重构期间残留的旧字段、旧会话 ID 和 fallback，并通过完整自动化与浏览器验证证明新模型可以作为后续 MCP 和 AI 测试空间的稳定基础。

**Blocked by:** 05 — 迁移 OneBot 与 ChatLuna 到逻辑会话；06 — 收紧 Console RPC 与未来 MCP 控制合同；07 — 统一 WebQQ 当前操作者视角

**Status:** ready-for-human

- [x] 默认场景包含可直接测试消息、关系申请和群权限的普通用户、机器人与群组，并只使用新参与者和逻辑会话结构
- [x] 测试 fixture、场景状态和浏览器本地状态不再包含旧双目录、旧身份字段、镜像私聊或成员机器人组合群聊
- [x] 项目中不存在为旧场景、旧 RPC、旧 localStorage 或旧会话 ID 提供的兼容解析、可选字段或 fallback
- [x] 回归测试覆盖用户与机器人操作者、三类私聊组合、多机器人群聊、关系删除恢复、退群重新入群和群主权限
- [x] 完整测试、类型检查、生产构建和差异检查全部通过
- [x] 使用真实浏览器验证操作者切换、会话列表、消息方向和主要交互，完成后关闭浏览器并清理临时文件

## Answer

- 默认场景固定为三个普通参与者、一个 NapCat 机器人和一个测试群；群主、管理员、普通成员与机器人管理员角色均直接可用，私聊和群聊均使用唯一逻辑会话
- 新增默认场景合同测试，锁定参与者目录、群成员角色、三个稳定用户-机器人私聊、唯一群会话、单份消息和默认好友申请入口
- 项目审计确认旧身份字段仅保留在 Console 入口的显式拒绝名单中；不存在旧场景、镜像私聊、旧 localStorage 迁移或旧会话 ID 兼容解析
- 全套回归已覆盖用户/机器人操作者、用户-用户/用户-机器人/机器人-机器人私聊、多机器人群消息投递、好友删除恢复、退群再入群、机器人群主转让与权限管理
- Ego Browser 已在 `http://127.0.0.1:5140/chatluna-sandbox` 验证当前操作者切换为 Koishi 后的会话、好友目录和右键操作；页面无控制台错误，浏览器已关闭且 `.playwright-mcp` 临时目录已清理
- Standards 审查 0 项问题；Spec 审查 0 项问题。当前会话禁止派生子代理，因此由主线程按同样两个维度核对
- 验证通过：`yarn test`（31 个文件、121 个测试）、`yarn typecheck`、`yarn build`、`git diff --check`
