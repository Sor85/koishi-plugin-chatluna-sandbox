# 媒体文件与场景状态分离存储

图片、头像、文件、语音和视频保存到插件受控的媒体存储，沙盒场景只记录安全引用和文件元数据，不向 Koishi Database 写入图片正文、外部地址或大块二进制。每个参与者和群组都关联一张头像图片；未显式设置时按实体种类和稳定 ID 生成并持久化默认图片，外部 URL、Data URL、`base64://` 和上传文件在写入前统一导入媒体存储。媒体按内容去重，并在场景重置、头像替换或其他引用消失后按引用关系回收无主文件。
