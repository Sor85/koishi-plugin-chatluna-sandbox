# 服务端持有沙盒状态

主模拟 QQ 环境和每个 AI 测试空间的沙盒场景都由服务端统一持有，默认使用 Koishi Database 作为跨重启持久化后端，并允许用户改用服务端内存。Database 保持可选注入：数据库缺失或查询失败时插件照常挂载，场景留在进程内且不覆盖数据库中的既有记录。WebQQ 外观由 Koishi 插件全局配置统一下发，浏览器只保存当前操作者、当前视图和已打开空间等客户端状态，避免插件 action、事件、MCP 和 WebUI 分别操作不一致的数据副本。
