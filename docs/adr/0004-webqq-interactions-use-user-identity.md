# WebQQ 交互默认使用用户身份

WebQQ 工作台保持当前用户视角，戳一戳、好友关系和群管理等人工交互仍由当前用户发起。消息发送控件可以临时选择当前会话的虚拟 OneBot 机器人作为发送者，用于直接构造机器人消息；这不会切换当前用户，也不会把该消息再次派发给机器人的 Koishi 中间件。机器人仍可通过当前 Koishi 实例中的插件调用 OneBot 能力，并将结果写入同一个模拟 QQ 环境。
