# 测试控制端点默认开启

测试控制端点的两种协议表述各有一个总开关，默认都开启，用户可以按需单独关闭。端点仍然只监听回环地址、仍然要求 Bearer 测试凭证，未创建任何测试凭证时所有请求一律 401，因此默认开启不等于默认可访问。端点配置或启动失败不得影响 WebQQ 工作台和沙盒主体，环境管理仅展示端点状态、地址和脱敏错误。

取代本决议原先「默认不注册也不监听、只有用户显式启用才启动」的结论：沙盒的用途就是被外部测试控制器驱动，把开箱可用的能力藏在一个默认关闭的开关后面，让每个用户都要先读文档才能开始用。代价是插件启用后即占用配置的端口，端口冲突时按既有路径降级为一条错误日志。
