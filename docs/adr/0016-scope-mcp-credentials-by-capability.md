# 测试凭证按能力范围授权

测试凭证使用可组合的 `read`、`interact`、`manage` 和 `debug` 权限控制工具发现与调用，默认只授予 `read`。凭证不按协议表述划分：同一个凭证在 MCP Streamable HTTP 与 HTTP 测试接口下通用，权限、配额与测试调用记录都是同一份，因此领域模型、Console 契约与界面文案都称它「测试凭证」而不是「MCP 凭证」。测试控制端点永远不允许修改凭证、监听地址、来源白名单或插件全局配置；AI 测试空间及其控制权限在基础 MCP 完成后按 ADR-0043 单独扩展。
