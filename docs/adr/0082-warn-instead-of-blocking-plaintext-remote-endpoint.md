# 非回环缺少 TLS 时告警而不拦启动

测试控制端点监听非回环地址且没有配置 TLS 证书和私钥时照常启动，只在日志里持续输出明文传输告警，不再要求用户先打开一个额外开关。该决定取代 ADR-0032：原先的门禁在缺 TLS 时拒绝启动，而唯一的解法是打开 `allowInsecureRemote`——一个除了解除门禁之外没有任何行为的开关，它把「是否接受明文」这件只有部署者知道的事变成了两步配置，并让第一次局域网调试必然先撞一次启动失败。凭证保护仍由 TLS 证书路径、来源 IP 与 Origin 白名单承担，明文风险仍必须在日志中可见。
