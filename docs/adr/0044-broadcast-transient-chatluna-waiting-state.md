# 单独广播 ChatLuna 等待态

ChatLuna 思考状态是不写入场景快照的瞬时状态，因此它变化时只发出场景变更广播，不递增场景修订，也不触发场景持久化。前端相应地按"收到广播"而不是"广播修订大于当前修订"决定是否重新拉取工作区，并把刷新期间到达的广播合并成一次后续拉取。

如果沿用修订号比较，等待态广播会因为修订未变而被前端跳过，聊天页面只能在下一条消息落库时刷新，那时思考早已结束，等待态永远不会显示。
