# 外部 AI 在独立测试空间中准备场景

外部测试控制器为每次测试创建一个空白且隔离的 AI 测试空间，而不直接修改主模拟 QQ 环境。测试空间属于当前沙盒实例，不再绑定创建它的 Bearer 凭证；任意有效测试凭证都可以发现并按自身 `read`、`interact`、`manage`、`debug` 能力范围访问全部空间，凭证轮换不会改变空间可见性。用户仍可在 WebQQ 接管和归还空间，接管期间暂停所有 MCP 修改；活动空间的机器人 ID 在 Koishi 实例内全局唯一，空间完成后停止机器人运行时并按插件全局持久化策略保留复盘数据。
