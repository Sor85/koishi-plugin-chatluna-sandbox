# 外部 AI 在独立测试空间中准备场景

外部测试控制器在 MCP 基础能力完成后，为每次测试创建一个空白且隔离的 AI 测试空间，而不直接修改主模拟 QQ 环境。每个空间显式使用 `spaceId`、同一时间只允许一个 AI 控制者、默认保留完成记录并遵循插件全局持久化策略；用户可在 WebQQ 实时观察、接管和归还，活动空间的机器人 ID 在 Koishi 实例内全局唯一，空间完成后停止机器人运行时。主环境的调试页和资料页可以提供带来源标识的跨空间只读汇总，但测试空间的场景、机器人和调试缓冲区仍由各自 control 持有，不写回主场景。
