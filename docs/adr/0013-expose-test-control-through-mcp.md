# 通过 MCP 暴露测试控制能力

WebQQ 与外部调用统一复用传输无关的测试控制服务，对外第一阶段只提供 MCP Streamable HTTP，使外部 AI 可以发现并调用结构化工具。CLI 和普通 REST 暂不实现，未来如有明确的人工脚本或非 AI 集成需求，再作为同一服务的薄适配层增加。

普通 HTTP 的需求随后出现，已按本决议预留的「同一服务的薄适配层」形态落地，见 ADR-0081；CLI 仍未实现。
