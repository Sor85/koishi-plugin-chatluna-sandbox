# OneBot Sandbox

本上下文描述用于在 Koishi 控制台内验证 OneBot 插件行为的虚拟运行环境。

## Language

**参与者**:
模拟 QQ 环境中具有账号资料、关系、群角色、会话可见性和消息身份的主体，通过参与者种类区分普通用户和虚拟 OneBot 机器人。
_Avoid_: 账号, QQ 实体, 特殊用户

**虚拟 OneBot 机器人**:
参与者的一种，仅存在于当前 Koishi 实例中，对已安装插件提供 OneBot action 与事件，并通过插件响应其他参与者的交互，但不接受外部 HTTP 或 WebSocket 连接。
_Avoid_: OneBot 服务端, 协议服务器, 假机器人

**实现配置**:
虚拟 OneBot 机器人采用的兼容行为基线，当前可选择 NapCat 或 LLBot，并决定可用 action 及其参数和返回差异。
_Avoid_: 平台, 协议版本, 主题

**能力覆盖**:
在实现配置默认能力矩阵上为单个虚拟 OneBot 机器人禁用已有能力的设置，用于验证插件的能力探测和降级行为。
_Avoid_: 自定义 Action, Mock 脚本, 实现配置

**当前操作者**:
WebQQ 工作台当前观察、操作和发送消息所使用的参与者，可以是普通用户或虚拟 OneBot 机器人；切换时最近会话、好友、群组、通知、权限和发送身份统一跟随该参与者，不存在独立的当前发送者。
_Avoid_: 当前用户, 当前发送者, 当前机器人

**逻辑会话**:
参与者之间唯一的私聊或群组对应的唯一群聊，独立于当前操作者和机器人事件接收者；私聊由稳定参与者对标识，群聊由群组标识。
_Avoid_: 用户会话, 机器人会话, 镜像会话, 用户机器人会话

**机器人事件投递**:
一条聊天消息向具体虚拟 OneBot 机器人产生的入站事件实例；同一条消息可以产生多次投递，但消息在逻辑会话中只保存一次。
_Avoid_: 机器人消息副本, Bot 会话, 消息镜像

**操作通道**:
当前操作者执行交互时采用的领域路径；普通用户通过用户交互直接作用于模拟 QQ 环境，虚拟 OneBot 机器人通过受启用状态、实现配置和能力覆盖约束的 OneBot action 执行。
_Avoid_: 权限路径, 模拟调用, 直接改状态

**用户目录**:
模拟 QQ 环境中可自由新增、删除和编辑的普通 QQ 用户集合，其中任意一个用户都可以被选为当前操作者。
_Avoid_: Koishi 用户表, 控制台账号, 群成员列表

**测试用户**:
由外部测试控制器在 AI 测试空间中为准备测试条件而创建的普通 QQ 用户，在模拟 QQ 环境中的行为与其他普通用户一致。
_Avoid_: AI 用户, 临时 Bot, 虚拟 OneBot 机器人

**机器人动作**:
虚拟 OneBot 机器人通过 WebQQ 当前操作者或当前 Koishi 实例中的插件调用 OneBot 能力而产生的操作，可以修改模拟 QQ 环境并影响相关用户和其他机器人。
_Avoid_: 用户操作, 控制台操作, 机器人配置

**机器人动作记录**:
对一次机器人动作的规范化观察结果，同时保留插件实际请求的 action、能力矩阵解析后的规范 action、脱敏参数及成功或失败结果，使不同实现配置和 action 别名可以使用同一套断言语义。
_Avoid_: 机器人动作, MCP 调用记录, 机器人回复

**环境管理**:
用于静默创建和整理用户、机器人、群组及关系的测试准备操作，不代表 QQ 参与者的真实行为，也不产生 OneBot 或 Koishi 事件。
_Avoid_: 用户操作, 机器人动作, 事件模拟

**关系申请**:
参与者之间发起的好友申请或入群邀请，在接收方明确同意或拒绝前保持待处理状态；普通用户从 WebQQ 处理，虚拟 OneBot 机器人通过插件 action 处理。
_Avoid_: 好友关系, 群成员关系, 环境管理

**OneBot 调试记录**:
用于观察 OneBot action、原始事件和错误的持久化只读证据，独立于沙盒场景和聊天消息，并按测试空间保留；它可以用于复盘和断言，但不能用于重放操作。
_Avoid_: 消息历史, 审计日志, Action 控制台, 沙盒场景

**外部测试控制器**:
通过对外测试接口创建并控制 AI 测试空间、选择普通用户身份、执行测试交互并读取结果的自动化调用者；它不是 QQ 参与者，也不能代替虚拟 OneBot 机器人执行动作。
_Avoid_: 外部 Bot, AI 用户, OneBot 客户端

**测试控制端点**:
供外部测试控制器连接并调用沙盒测试能力的 MCP Streamable HTTP 入口。
_Avoid_: OneBot 服务端, REST API, WebQQ RPC

**测试凭证**:
授予外部测试控制器访问测试控制端点及特定能力范围的具名 Bearer 凭证；凭证决定调用权限，但不拥有或隔离 AI 测试空间。
_Avoid_: 登录账号, OneBot Token, 全局密码, 空间所有者

**事件游标**:
外部测试控制器用于从某个已知位置继续等待沙盒事件的标识，避免把调用前的旧事件误认为当前测试结果。
_Avoid_: 消息 ID, 分页游标, 调试记录序号

**调试记录游标**:
外部测试控制器用于按稳定顺序继续读取 OneBot 调试记录的分页位置，与短期事件等待使用的事件游标相互独立；游标对应的记录被容量策略回收后即过期。
_Avoid_: 事件游标, Offset, 消息 ID

**测试关联标识**:
由外部测试控制器提供、用于把多次命令及其后续消息和事件归入同一次测试的可选标识，不表示测试状态或结果。
_Avoid_: 幂等 Key, 事件游标, 测试用例 ID

**测试调用记录**:
用于追踪外部测试控制器调用工具的脱敏内存记录，保留凭证、来源、工具、空间、参数、结果与错误，与 QQ 消息、OneBot 调试记录和模型请求记录相互独立，且不能用于重放操作。
_Avoid_: OneBot 调试记录, HTTP 访问日志, 测试报告, MCP 调用日志

**机器人目录**:
沙盒场景中可自由新增、删除、启用和停用的虚拟 OneBot 机器人集合。
_Avoid_: 固定机器人槽位, 双机器人配置, 账号列表

**模拟 QQ 环境**:
沙盒场景中由所有虚拟 OneBot 机器人共同参与的用户、好友关系、群组、成员关系、权限和消息环境；不同机器人根据自身关系看到其中不同的内容。
_Avoid_: 模拟 QQ 世界, 独立机器人数据副本, QQ 网络

**好友备注**:
参与者为自己的好友设置的本地名称，不改变对方的资料昵称或群名片。
_Avoid_: 好友昵称, 用户昵称, 群昵称

**群名片**:
参与者在某个群组中的成员名称，同一参与者在不同群组中可以拥有不同群名片。
_Avoid_: 用户昵称, 好友备注, 群昵称

**专属头衔**:
群主授予某个群成员的荣誉称号，与群名片和群身份相互独立；在消息徽标中覆盖群身份文案，但仍沿用群身份配色。
_Avoid_: 群名片, 群身份, 用户昵称

**群禁言状态**:
群成员当前被禁止发言的截止时间，到期后自动失效；解除禁言表示清除该截止时间，而不是记录一次解禁动作。
_Avoid_: 全员禁言, 黑名单, 踢出群组

**表情回应**:
参与者对一条未撤回消息贴出的 emoji 反馈，私聊与群聊使用同一回应事实，按 emoji 聚合参与者，同一参与者对同一 emoji 只计一次；消息撤回后保留已有回应作为历史事实，但不再接受新增或取消。
_Avoid_: 消息引用, 撤回, 表情消息

**消息生命周期状态**:
消息当前处于可见或已撤回阶段的结构化状态；撤回后保留消息身份、会话位置和原始内容供 WebQQ 展示与测试复盘，并记录撤回者和撤回时间，但普通 OneBot 查询不再读取原始内容。
_Avoid_: 系统消息, 消息事件, 删除消息, WebQQ 展示模式

**合并转发资源**:
由有序消息节点组成的独立场景资源；外层消息只保存转发资源 ID 与预览摘要，完整节点列表（作者 ID/昵称快照、正文、媒体、时间与嵌套转发引用）保存在场景 `forwards` 集合中，可被 `get_forward_msg` 与 WebQQ 详情展开。
_Avoid_: 文本展平, 普通多行消息, 仅前端卡片状态

**合并转发节点**:
合并转发资源中的一条消息快照，可以引用已有可见消息，也可以由 OneBot 自定义 `user_id`/`nickname`/`content` 生成；删除参与者或改名不影响历史节点。
_Avoid_: 实时消息对象, 会话消息副本

**头像图片**:
每个参与者和群组关联的持久化图片资源；普通用户、虚拟 OneBot 机器人和群组未显式设置时分别从各自的 8 个内置头像中随机分配，同一沙盒场景、同一实体种类在池耗尽前不重复，分配后改名不会改变头像；场景只记录媒体引用而不保存图片正文或外部地址。
_Avoid_: 头像字符串, 外部头像 URL, Base64 头像, 名称首字占位

**账号资料**:
普通用户或虚拟 OneBot 机器人跨好友关系和群组保持一致的全局个人信息，包括昵称、头像图片、个性签名及实现配置能够表达的其他可选资料；好友备注和群成员资料不属于账号资料。
_Avoid_: 好友备注, 群名片, 群成员资料, 机器人运行配置

**个人信息卡**:
WebQQ 工作台汇总展示某个普通用户或虚拟 OneBot 机器人的账号资料、好友关系资料、群成员资料和机器人运行资料的只读视图，并明确标注每项信息的所属范围。
_Avoid_: 私聊信息面板, 环境编辑表单, OneBot 原始响应

**机器人资料**:
虚拟 OneBot 机器人自身的账号资料；机器人可以通过插件调用当前实现配置支持的 OneBot 能力修改昵称、个性签名及其他受支持字段。
_Avoid_: 好友资料, 当前操作者, Bot 配置

**群名称**:
群组自身的名称，对有权查看该群组的所有机器人和成员一致可见。
_Avoid_: 群昵称, 群名片, 会话名称

**WebQQ 工作台**:
从 Koishi 左侧导航进入并铺满内容区域的交互式 OneBot 沙盒页面，以单一当前操作者统一展示模拟数据、判断权限和触发操作。
_Avoid_: WebQQ 观察窗, 胶囊窗口, 弹窗

**ChatLuna 对话状态**:
WebQQ 工作台中用于验证 ChatLuna 对话生命周期的状态信息，仅包含思考状态、归属于具体机器人消息的模型思考内容和模型 Token 用量；消息撤回不删除已归档状态，WebQQ 是否展示由撤回呈现方式决定。
_Avoid_: ChatLuna 完整联动, 好感度状态, 关系状态, 日程状态, 独立思考消息

**模型请求记录**:
对一次实际上游对话模型 HTTP 交互的持久化只读证据，保存最终请求体、原始响应体、规范化请求地址、调用状态、可验证的空间归属，以及能够唯一关联时由 ChatLuna 错误事件补充的规范错误码、错误消息和原始原因，供 WebQQ 复盘及外部测试控制器分析；流式响应保留完整 SSE 原文并在读取时派生事件预览，无法安全归属的请求或 ChatLuna 错误进入独立分类或保持未关联，而不猜测所属空间。
_Avoid_: ChatLuna 对话状态, 提示词快照, HTTP 访问日志, OneBot 调试记录

**模型请求轨迹**:
从模型请求记录读取时派生的语义化复盘视图；单请求轨迹按模型实际收到的消息、工具目录和实际响应组织记录，会话轨迹只将同一空间且同一逻辑会话的多次模型请求按时间串联为步骤，并明确保留当前证据无法证明的工具执行、压缩、子任务与细粒度计时为空。
_Avoid_: DeepSeek Harness Session Event, ChatLuna 对话状态, 模型请求记录副本, 推测的工具执行日志, 沙盒场景事件流

**模型请求对话视图**:
对单条模型请求记录进行阅读和复盘的消息视图，将请求上下文、模型响应和工具目录分开呈现；它保留消息及响应的原始顺序和来源边界，不把工具定义误认为对话消息，也不把模型响应伪装成请求上下文。
_Avoid_: 模型请求轨迹, ChatLuna 对话状态, 聊天记录, 推测的对话线程

**请求消息**:
模型请求发送前实际包含在请求体中的上下文消息，可以来自不同协议的 system、user、assistant 和 tool 角色；消息内容应保持原始分片顺序，并能够回溯到模型请求记录中的原始证据。
_Avoid_: 用户消息, 沙盒消息, 模型响应, 工具定义

**模型响应**:
上游模型针对一次模型请求返回的实际证据，包括可见正文、思考内容、工具调用、工具结果、结束原因和可用的 Token 用量；它与请求消息分开观察，不等同于新增的沙盒消息。
_Avoid_: assistant 消息, 请求消息, 推测的工具执行结果, ChatLuna 对话状态

**工具定义**:
模型请求中声明的可调用工具及其描述和参数 Schema，属于模型请求的能力目录而不是运行时消息；工具定义应按原始声明顺序保留，并可回溯到对应的请求证据。
_Avoid_: 工具调用, 工具结果, MCP 工具注册, 机器人动作

**工具调用**:
请求消息或模型响应中要求调用某个工具的结构化内容，包含工具名称、调用标识和参数；它属于 assistant 或模型响应的工具交互，不等同于工具已经执行成功。
_Avoid_: 工具定义, 工具结果, 机器人动作记录, 已执行工具

**工具结果**:
请求消息或模型响应中明确返回的工具执行结果，只通过明确的调用标识与工具调用关联；缺少调用标识时不按工具名称或出现位置推断配对，缺少实际证据时不推断工具结果。
_Avoid_: 工具调用, 工具定义, 推测结果, 机器人动作, 按名称配对的工具结果

**原始模型证据**:
模型请求记录中与规范化请求消息、模型响应或工具定义对应的未改写数据及其来源路径，用于在格式化视图和原始内容之间核对，不表示重新发起请求或重新解析得到的新事实。
_Avoid_: HTTP 原始日志, 数据库快照, 推测内容, 页面缓存

**证据定位**:
在模型请求对话视图与模型请求轨迹之间跳到某条原始模型证据、按需展开它所在的卡片与长文本，并在返回时恢复原视图与滚动位置的行为；它不改变证据本身，也不产生新的证据。
_Avoid_: 消息列表贴底追踪, 滚动条状态, 展开状态, 模型证据投影

**预设文档**:
ChatLuna 核心预设或 Character 预设在各自权威目录中的一份 YAML 源文件；核心预设与 Character 预设属于不同文档种类，文件名只标识文档，不等同于运行时预设名称。
_Avoid_: 提示词快照, 模型请求记录, 角色配置表, 旧 TXT 预设

**运行时预设快照**:
一次 ChatLuna 对话回合实际使用的预设名称和原始模板单元的只读副本，按空间、虚拟 OneBot 机器人和逻辑会话关联到模型请求，用于证明源码模板身份，但不替代模型实际收到的请求证据。
_Avoid_: 预设文档副本, 最终提示词, 模型请求体, 本地渲染预览

**预设证据位置**:
预设文档中的一个值表达式在某条最新匹配模型请求的请求消息正文中可被唯一证明的 UTF-16 文本范围；该范围内的原文可以作为表达式观察值展示，控制标签、歧义结果和未观察到的表达式不产生预设证据位置。
_Avoid_: 变量当前值, 卡片定位, 字符串搜索结果, 推测展开结果

**沙盒场景**:
某个隔离模拟 QQ 环境的权威状态，由参与者及其好友、群组、逻辑会话、消息、通知和权限关系共同构成；主模拟 QQ 环境与每个 AI 测试空间分别拥有一份沙盒场景。
_Avoid_: 浏览器状态, 页面缓存, 测试数据, 全局唯一场景

**主模拟 QQ 环境**:
用户日常通过 WebQQ 工作台维护和操作的默认沙盒场景，不接受外部测试控制器直接写入。
_Avoid_: 默认空间, 用户空间, 主场景

**AI 测试空间**:
外部测试控制器为一次测试创建的隔离沙盒场景，属于当前沙盒实例并由所有有效测试凭证按各自能力范围共享访问；用户可以通过 WebQQ 实时观察、接管和复盘。
_Avoid_: AI 用户, 临时场景, MCP Session, 测试关联标识, 凭证私有空间

**空间控制状态**:
AI 测试空间当前由外部测试控制器修改或由用户接管的状态；用户接管时暂停所有测试凭证的修改能力，归还后恢复按能力范围授权的共享控制。
_Avoid_: MCP Session, 当前操作者, 测试凭证所有权, 空间控制租约

**默认场景**:
插件首次启用或用户主动重置时生成的开箱即用沙盒场景，包含可立即验证消息、关系申请和群权限的机器人、用户、群组及示例状态。
_Avoid_: 演示数据, 固定场景, 数据库默认值
